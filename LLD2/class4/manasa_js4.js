// function convertTemperature(temp, unit) {
//     if (unit === "C") {
//         const fahrenheit = (temp * 9/5) + 32;
//         return `${temp}°C is ${fahrenheit}°F`;
//     } else if (unit === "F") {
//         const celsius = (temp -32) * 5/9;
//         return `${temp}°F is ${celsius}°C`;
//     } else {
//         return `Invalid unit.use "C" or "F"`;
//     }
// }

// console.log(convertTemperature(100, "C"));

// function reverseWords(str) {
//   let result = str.split(" ");

//   for (let i = 0; i < result.length; i++) {
//     let word = result[i];
//     let reversedword = "";

//     for (let j = 0; j < word.length; j++) {
//       reversedword = word[j] + reversedword;
//     }
//     result[i] = reversedword;
//   }
//   return result.join(" ");
// }

// console.log(reverseWords("hello world!"));

// function palindrome(str) {

//     let l = 0;
//     let r = str.length - 1;

//     for (let i = 0; i < str.length / 2; i++) {
//         if (str[l] !== str[r]) {
//             return false;
//         }
//         l++;
//         r--;
//     }
// }

// fetch score of individual students and return or push only name it to the array according to categories

// function categorizeStudents(students) {
//   const toppers = [];
//   const cool = [];
//   const supercool = [];

//   for (let i = 0; i < students.length; i++) {}
// }

// createEncoder

// if str = "abc" and createencoder is 1 then increment each index value by 1 and return new string

// function createEncoder(shift) {
//     return encode1(str) {
//         let result = "";
//         for (let i = 0; i < str.length; i++) {
//             const char = str[i];
//             const index =
//         }
//     }
// }

// const encode1 = createEncoder(1);
// console.log(encode1("abc")); // Output: "bcd"
