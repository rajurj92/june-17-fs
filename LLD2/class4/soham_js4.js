//Q1. Write a function called convertTemp that takes two arguments:
// a number (the temperature) and a string
// (the unit, either "C" or "F"). If the unit is "C", convert the temperature to Fahrenheit.
// If the unit is "F", convert it to Celsius. Return a string that describes the result.

// function convertTemp(temp,deg) {
//     if(deg === 'C'){
//         const farenheit = (temp * 9/5) + 32;
//         return `${temp}deg C is ${farenheit}deg F`
//     }
//     else if(deg === 'F'){
//         const celcius = (temp - 32) * 5/9;
//         return `${temp} deg F is ${celcius} deg C`
//     }
//     else{
//         return `invalid unit. Use deg C or deg F`
//     }
// }

//Q2.  Write a function called reverseWords that takes a sentence (a string) and
// returns a new string where each word is reversed, but the word order stays the same.

// function reverseWords(sentence){
//     const words = sentence(" ")

//     const len = result.length;

// }

// function categorizeStudents(students){
//     const result = {
//         toppers: [],
//         cool: [],
//         superCool:[]
//     }
//     students.map((item) => {
//         if(item.score >= 80){
//             result.toppers.push(item.name);
//         }
//         else if(item.score >= 50 && item.score <= 79){
//             result.cool.push(item.name);
//         }
//         else if(item.score < 50){
//             result.superCool.push(item.name);
//         }
//     })
// }

const encode3 = createEncoder(3);
const newArray = [];
const arr = "abcdefghijklmnopqrstuvwxyz";
function createEncoder(val) {
  return;
}
